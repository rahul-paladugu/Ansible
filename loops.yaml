- name: Loops in ansible
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.dnf:
     name: "{{ item }}"
     state: latest
    loop:
     - nginx
     - mysql
     - zip
